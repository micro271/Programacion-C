#include <stdio.h>
int main()
{
    //nu Cidugi
    int x,y,suma;
    float pi;

    x = 3;
    y = 5;
    suma = x + y;
    printf("x = %d & y = %d \n",x,y);
    printf("x + y = %d",suma);
    return 0;
}
