#include<stdio.h>
int main(void)
{
    float base,altura,area;
    base = 5.45;
    altura = 17.32;
    area = (base*altura)/2;
    printf("base: %g, altura: %g, area: %g",base,altura,area);
    return 0;
}
