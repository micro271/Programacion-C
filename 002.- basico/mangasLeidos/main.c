/*
		**Un programa para poder gaurdar organizar buscar un manga leido por el usuario
		obviamente podemos intruducir mangas y guardarlos en uns pocicion que se decee
*/
#include <stdio.h>
#include <stdlib.h>
#include "funciones.h"
int main(int argc, char **argv) {
	char binario[] = "data.dat";
	menu(binario);
	return 0;
}
